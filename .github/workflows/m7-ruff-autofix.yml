name: M7 Temporary Ruff Autofix

on:
  push:
    branches: [feat/m7-prepared-artifacts-replay]

permissions:
  contents: write

jobs:
  autofix:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/m7-prepared-artifacts-replay
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - name: Install tooling
        run: |
          python -m pip install --upgrade pip
          pip install -e '.[dev]'
      - name: Apply Ruff safe fixes and formatting
        run: |
          ruff check --fix src tests
          ruff format src tests
      - name: Commit mechanical cleanup
        run: |
          if git diff --quiet; then
            echo 'No autofix changes required.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add src tests
          git commit -m 'style: apply M7.1 Ruff cleanup'
          git push origin HEAD:feat/m7-prepared-artifacts-replay
