# M8.1 — AccessZone / TTL Completion & Qualification

## Status

**Milestone:** M8.1 AccessZone / TTL durable lineage  
**State:** IMPLEMENTED — CI QUALIFICATION PENDING  
**Base:** `main` after PR #33  
**Next milestone:** M8.2 AstraVector Wire Adapter

M8.2 MUST NOT start until the CI gates for this change are green.

## Qualification invariants

| Requirement | Implementation | Evidence |
|---|---|---|
| Preserve four-digit AccessZone code including leading zeroes | `AccessZoneCode` | `test_m8_1_accesszone_ttl_lineage.py` |
| Accept Spring singular/plural compatibility fields | `normalize_delivery_intent` | `test_m8_1_boundary_normalization.py` |
| Normalize one effective zone; never fan out | boundary normalizer conflict/distinct checks | missing/ambiguous/distinct tests |
| Preserve ID + code as correlation assertion | `AccessZoneIntent` + normalizer | correlation tests |
| `ttlDays=0` means inherit; positive means finite | `TtlIntent` | TTL tests |
| Reject negative/non-integer TTL | boundary normalizer | invalid TTL matrix |
| Persist requested selector independently of legacy columns | `NewIndexationJob` + repository `requested_*` writes | PostgreSQL qualification tests |
| Support UUID-only producer selector | migration makes legacy code nullable | PostgreSQL UUID-only test |
| Upgrade a non-empty pre-M8 database safely | migration backfill before presence CHECK | migration qualification test |
| Snapshot AccessZone/TTL into M7 checkpoint | `PreparedArtifactCheckpointService` | M7/M8 lineage tests |
| Preserve intent after restart/replay | durable checkpoint requested fields | M7 checkpoint PostgreSQL suite |
| Keep resolved AstraVector identity separate | `resolved_access_zone_id` | migration/model contract |

## Boundary normalization contract

Accepted producer compatibility fields:

```text
accessZoneId      UUID?
accessZoneIds     UUID[]?
accessZoneCode    string?
accessZoneCodes   string[]?
ttlDays           integer? (default 0)
```

Rules:

1. Singular and plural aliases may coexist only when they identify the same single value.
2. Duplicate values in a plural alias collapse to one effective selector.
3. Multiple distinct IDs or multiple distinct codes are rejected.
4. ID and code may coexist. AstraIndexator preserves both as a correlation assertion; AstraVector resolves authority in M8.2+.
5. No selector is rejected.
6. AccessZone codes are strings, never integers.
7. `ttlDays` rejects booleans, floats, strings and negative integers.

## Migration safety correction

The initial M8.1 migration created the `requested_access_zone_selector_present` CHECK before backfilling existing rows. PostgreSQL validates a new CHECK against existing rows immediately, so any non-empty M1 database could fail the upgrade.

The qualified ordering is:

```text
add requested columns
→ add format constraints
→ backfill existing jobs
→ enforce requested selector presence
→ relax obsolete legacy code NOT NULL for UUID-only selectors
```

The same ordering is applied to prepared-artifact checkpoint lineage.

## Exit gates

M8.1 becomes **QUALIFIED** only when all are green on the PR head:

```text
pytest
PostgreSQL/Testcontainers integration
Alembic upgrade evidence from non-empty 0003 database
Ruff lint
Ruff format check
mypy
package build
```

A failed or absent CI run means `IMPLEMENTED — CI QUALIFICATION PENDING`, not QUALIFIED.
