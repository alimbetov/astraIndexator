# M8.2.8 — Status / SEARCHABLE Readiness Qualification

## Status

**M8.2.8:** QUALIFIED  
**Qualification CI:** AstraIndexator CI #277 — SUCCESS  
**Qualification head:** `0d06a94f10f7fc63431a413cc3f4415ddda0c4c7`  
**Branch:** `feat/m8-2-astravector-wire-adapter`  
**Previous gate:** M8.2.7 QUALIFIED by AstraIndexator CI #274

## Purpose

M8.2.8 qualifies the public AstraVector read-side contract used after Finalize. The core invariant is:

```text
IngestionSession COMPLETED != document SEARCHABLE
```

Finalize/session completion only proves that AstraVector accepted the logical document and entered its asynchronous vector lifecycle. AstraIndexator may claim retrieval readiness only from `GetDocumentVectorStatus` evidence.

## Pinned wire authority

The generated client remains pinned to AstraVector proto blob:

```text
ed1eab5f56dfb73cc48927ad2effb759a2c4e01e
```

The public facade exposes:

```text
GetDocumentVectorStatus(GetDocumentVectorStatusRequest)
  -> GetDocumentVectorStatusResponse
```

with exact `DocumentRef`, `OperationState`, `progress_percent`, `searchable`, `ready_to_activate`, and `GetVectorSyncStatusResponse` evidence.

CODEX-10 extends the applied runtime contract with public activation evidence from:

```text
AstraVectorV004Control.ActivateDocumentVersion(ActivateDocumentVersionRequest)
```

This is still a generated public AstraVector client boundary; no private AstraVector PostgreSQL or Qdrant access is permitted.

## Readiness state machine

AstraIndexator interprets the public status as follows:

```text
ACCEPTED / INDEXING / VECTORING / PUBLISHING / SYNCING
    -> WAIT / FINALIZED

READY_TO_ACTIVATE + ready_to_activate=true + consistent sync evidence
    -> WAIT / VECTOR_READY by default
    -> ActivateDocumentVersion under the current MANUAL activation policy
    -> READY_TO_ACTIVATE only when an explicit external/manual activation owner allows handoff

ACTIVE + searchable=true + consistent sync evidence
    -> SEARCHABLE

READY_TO_ACTIVATE + searchable=true + sync.documentStatus=ACTIVE + consistent sync evidence
    -> SEARCHABLE

FAILED / EXPIRED / DELETED / DELETE_SCHEDULED / DELETING
    -> TERMINAL
```

Unknown operation states fail closed.

`READY_TO_ACTIVATE` alone is deliberately not equivalent to `SEARCHABLE`. After CODEX-10, AstraIndexator calls the public generated `ActivateDocumentVersion` RPC for MANUAL activation and then accepts `READY_TO_ACTIVATE + searchable=true + sync.documentStatus=ACTIVE` as searchable completion when all sync counters are consistent.

## Integrity requirements

A ready/searchable response is rejected when any of the following contradicts it:

- `synced_bindings != expected_bindings` when expected bindings are non-zero;
- pending or failed bindings remain;
- pending/retry/failed outbox work remains;
- Qdrant reports missing expected points;
- found Qdrant points are fewer than expected points;
- `ACTIVE` reports `searchable=false`;
- `READY_TO_ACTIVATE` reports `ready_to_activate=false`;
- `READY_TO_ACTIVATE` reports `searchable=true` without `sync.documentStatus=ACTIVE`;
- a transitional or terminal state reports readiness flags;
- any sync counter is negative;
- progress is outside `0..100`;
- response `DocumentRef` differs from the requested access-zone/document/version identity.

The adapter therefore treats status evidence as an integrity contract, not as advisory telemetry.

## Real generated-gRPC evidence

`tests/test_m8_2_8_searchable_readiness.py` adds a real in-process generated-gRPC round trip:

```text
generated GetDocumentVectorStatusRequest
 -> generated Stub
 -> grpc.Channel
 -> generated AstraVectorIngestionFacadeServicer
 -> generated GetDocumentVectorStatusResponse
 -> AstraVectorGrpcAdapter
 -> evaluate_vector_readiness
```

It proves the requested `access_zone_id`, `document_id`, `document_version`, `include_qdrant=true`, returned sync counters and Qdrant evidence reach the SEARCHABLE decision without a parallel hand-written wire DTO.

## Boundary with later milestones

M8.2.8 qualifies interpretation of authoritative status observations. CODEX-10 adds the activation-aware completion interpretation and durable runner tests, but the original historical CI reference above remains the M8.2.8 point-in-time qualification.

Those concerns remain outside this step:

- durable delivery execution -> M8.3;
- local/remote state reconciliation and SEARCHABLE confirmation over time -> M8.4;
- real retrieval proof -> CODEX-10/B2 qualification evidence.

## Qualification result

AstraIndexator CI #277 passed the complete qualification gate:

```text
Generate pinned AstraVector gRPC client  PASS
Ruff lint                               PASS
Ruff format check                       PASS
M8 scoped mypy                          PASS
package build                           PASS
full pytest                             PASS
PostgreSQL integration tests            PASS
M8.2.8 SEARCHABLE readiness tests       PASS
```
